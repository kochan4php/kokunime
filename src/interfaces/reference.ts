export interface Recommendation {
  title?: string;
  endpoint?: string;
  image?: string;
  url?: string;
}

export interface Genre {
  name: string;
  url?: string;
  endpoint?: string;
}

export interface Season {
  name: string;
  endpoint?: string;
  url?: string;
}
